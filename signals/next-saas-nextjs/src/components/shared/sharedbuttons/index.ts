/**
 * ActionButtons - Shared high-converting CTA buttons
 *
 * Usage:
 * import { ActionButton } from '@/components/shared/sharedbuttons';
 * import { UrgentCountdownButton } from '@/components/shared/sharedbuttons';
 *
 * <ActionButton variant="urgent-countdown" onClick={handleTrade} />
 * <UrgentCountdownButton onClick={handleTrade} />
 */

export {
  ActionButton,
  UrgentCountdownButton,
  LivePulseButton,
  ProfitAlertButton,
  RocketLaunchButton,
  type ActionButtonVariant,
} from './ActionButtons';

export { default } from './ActionButtons';