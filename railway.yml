# Railway Configuration - Deploy to: https://railway.app
# Railway + Supabase: Railway hosts the app, Supabase handles database

# Railway will auto-detect the Dockerfile and use it for deployment
# No need for explicit build/start commands when using Dockerfile

# Environment Variables (set in Railway dashboard):
# NODE_ENV=production
# PORT=5001
# NEXT_PUBLIC_SUPABASE_URL=https://avxygvzqfyxpzdxwmefe.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_rtWhC7Pp5XzJCTFTTR6VzQ_FIYgdOTQ
# SUPABASE_SERVICE_ROLE_KEY=sb_secret_Ci7ZL06_YdZFdoB9h3SmFg_cX3INlEp

# Alternative: Railway PostgreSQL Setup
# If you want Railway-managed database instead of Supabase:
# 1. Add PostgreSQL service in Railway dashboard
# 2. Copy connection string to environment variables:
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# POSTGRES_HOST=${{Postgres.PGHOST}}
# POSTGRES_USER=${{Postgres.PGUSER}}
# POSTGRES_PASSWORD=${{Postgres.PGPASSWORD}}
# POSTGRES_DATABASE=${{Postgres.PGDATABASE}}
# POSTGRES_PORT=${{Postgres.PGPORT}}

# Setup Instructions:
# 1. Go to railway.app and connect your GitHub repo
# 2. Choose: Keep Supabase OR add Railway PostgreSQL
# 3. Set environment variables in Railway dashboard
# 4. Deploy - Railway handles the rest automatically